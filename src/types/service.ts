export interface IServiceFormData {
  title: string;
  shortDescription: string;
  description: string;
  category: string;
  price: string;
  location: string;
  image: string;
  providerId?: string;
}